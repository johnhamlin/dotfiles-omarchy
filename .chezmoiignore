# Always ignored
.config/nvim/lazy-lock.json
packages-explicit.txt
packages-aur.txt
MIGRATION.md
windows-terminal-settings.json
wezterm.lua
powertoys-keyboard-manager.json

# Omarchy owns nothing in .config/hypr — chezmoi manages these:
# hyprland.conf, monitors.conf, hypridle.conf (all real files in source).
# HyDE-era leftovers in the deployed dir (userprefs.conf, keybindings.conf,
# windowrules.conf, nvidia.conf) are unmanaged and removed in demolition.

{{ if eq .chezmoi.os "windows" }}
# Linux-only
.config/hypr-custom/
.config/kitty/
.config/fish/
.config/alacritty/
.config/ghostty/
{{ end }}

{{ if ne .chezmoi.os "windows" }}
# Windows-only
.glzr/
{{ end }}

{{ if contains "microsoft" .chezmoi.kernel.osrelease }}
# WSL — exclude desktop-environment-specific files
.config/hypr-custom/
.config/hyde/
.config/kitty/
.config/alacritty/
.config/ghostty/
.config/vivaldi-stable.conf
.local/bin/
.local/share/applications/
{{ end }}

{{ if ne .formFactor "laptop" }}
.local/bin/power-mode
.local/bin/power-brightness
{{ end }}
