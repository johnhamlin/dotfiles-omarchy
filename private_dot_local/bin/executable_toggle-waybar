#!/bin/bash

# Toggle waybar visibility — delegate to HyDE's hyde-shell if available

if command -v hyde-shell >/dev/null 2>&1; then
  exec hyde-shell waybar --hide
fi

# Fallback: simple toggle
if pgrep -x waybar >/dev/null; then
  pkill -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
