# Application launchers.
#
# This is our equivalent of Omarchy's ~/.config/hypr/bindings.conf — a USER
# file upstream, so nothing in the sourced default chain binds these combos
# and no `unbind` is needed here. Keep it that way: anything that collides
# with an Omarchy default belongs in overrides.conf, not this file.
#
# Helper scripts are Omarchy's own (omarchy-cmd-terminal-cwd,
# omarchy-launch-browser, omarchy-launch-or-focus, omarchy-launch-editor,
# omarchy-launch-tui). We used to carry byte-identical forks of the first
# three in ~/.local/bin under un-prefixed names; those were deleted
# 2026-08-12 — see ~/notes/keybindings-audit-2026-08-12.md.

# Terminal
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux

# Browser
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private

# File manager
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"

# Applications
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# Web apps
bindd = SUPER SHIFT, A, Claude, exec, omarchy-launch-webapp "https://claude.ai/"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
