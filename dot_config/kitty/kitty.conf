# Managed by chezmoi. HyDE's kitty.conf is gone; this file owns the include chain.
# Omarchy theme colors (regenerated by omarchy-theme-set)
include ~/.config/omarchy/current/theme/kitty.conf

# Font
font_family JetBrainsMono Nerd Font
bold_italic_font auto
font_size 9.0

hide_window_decorations yes
confirm_os_window_close 0
enable_audio_bell no

# John's config: fish shell, Ctrl+A leader table, smart-splits, padding
include custom.conf
